🗄️ Backup Distribuído PostgreSQL com pgBackRest e MinIO

Este repositório documenta a instalação, configuração e validação de uma arquitetura de backup distribuído para bancos de dados PostgreSQL, utilizando o pgBackRest integrado a um armazenamento em objetos compatível com S3, baseado no MinIO.

O projeto foi desenvolvido como estudo de caso em ambiente controlado, representativo de um cenário real de grande escala, com o objetivo de servir como base técnica para futura implantação em produção.
